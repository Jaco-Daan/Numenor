# LotR: WIP edited real-world references.

namespace = numenor_historical_artifacts
# Generation of historical artifacts #

#Historical artifacts generator at game start
numenor_historical_artifacts.0001 = {
	scope = none
	hidden = yes

	immediate = {
		## Númenor Submod ##
		if = {
			limit = {
				current_date >= 542.9.10
			}
			title:e_numenor.holder  = { # King of Numenor receives Dramborleg
				create_artifact_dramborleg_effect = { OWNER = this }
				create_artifact_scroll_of_kings_effect = { OWNER = this }
			}
		}
		
		if = {
			limit = {
				current_date >= 4115.1.1
			}
			title:e_numenor.holder  = { # King of Numenor receives Little Aranruth
				create_artifact_nibaran_effect = { OWNER = this }
			}
		}

		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 542.9.10
			}
			title:e_numenor.holder  = { # King of Numenor receives Scroll of Kings
				create_artifact_scroll_of_kings_effect = { OWNER = this }
			}
		}
		
		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 1063.1.1
			}
			title:e_numenor.holder  = { # King of Numenor receives Palantir of Romenna
				create_artifact_palantir_romenna_effect = { OWNER = this }
			}
		}

		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 3912.5.3
			}
			title:e_numenor.holder  = { # King of Numenor receives Numenorean Figurine
				create_artifact_numenorean_figurine_effect = { OWNER = this }
			}
		}
		
		
		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 3912.5.3
			}
			title:e_numenor.holder  = { # King of Numenor receives Numenorean Helm
				create_artifact_numenorean_helm_effect = { OWNER = this }
			}
		}

		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 3912.5.3
			}
			title:e_numenor.holder  = { # King of Numenor receives Numenorean Shield
				create_artifact_numenorean_shield_effect = { OWNER = this }
			}
		}
		
		if = {
			limit = {
				has_dlc_feature = royal_court
				current_date >= 3912.5.3
			}
			title:e_numenor.holder  = { # King of Numenor receives Numenorean Steelbow
				create_artifact_numenorean_steelbow_effect = { OWNER = this }
			}
		}
	}
}
