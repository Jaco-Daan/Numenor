bribe_council_member_interaction = {
	icon = icon_gold
	category = interaction_category_friendly
	common_interaction = yes
	interface_priority = 60
	desc = bribe_council_member_interaction_desc

	greeting = positive
	notification_text = SEND_GIFT_PROPOSAL

	answer_accept_key = SEND_GIFT_ACCEPT
	answer_reject_key = SEND_GIFT_REJECT

	ai_targets = {
		ai_recipients = scripted_relations
	}
	ai_targets = {
		ai_recipients = liege
	}
	ai_targets = {
		ai_recipients = head_of_faith
	}
	ai_targets = {
		ai_recipients = neighboring_rulers
		ai_recipients = peer_vassals
		max = 10
	}
	ai_targets = {
		ai_recipients = vassals
		max = 10
	}
	ai_target_quick_trigger = {
		adult = yes
	}
	ai_frequency = 12

	is_shown = {
		NOT = { scope:recipient = scope:actor }

		OR = {
			scope:recipient = title:d_nindamos.holder
			scope:recipient = title:d_hyarnudore.holder
			scope:recipient = title:d_sorontil.holder
			scope:recipient = title:d_formenore.holder
			scope:recipient = title:d_andunie.holder
			scope:recipient = title:d_nisimaldar.holder
			scope:recipient = title:d_nunduine.holder
			scope:recipient = title:d_ciryanore.holder
		}
		NOT = {
			is_target_in_global_variable_list = {
				name = unavailable_unique_decisions
				target = flag:disband_numenor_council
			}
		}

		scope:recipient = {
			opinion = {
				target = scope:actor
				value < 1
			}
		}
	}

	is_valid_showing_failures_only = {
		scope:actor.gold >= gift_value
		scope:recipient = {
			is_busy_in_events_localised = yes
			NOT = {
				has_trait = honest
				has_trait = just
			}
		}
	}

	on_auto_accept = {
		scope:recipient = {
			trigger_event = {
				id = numenor.3310
				days = 1
			}
		}
	}

	on_accept = {
		custom_tooltip = bribe_council_member_interaction_desc
	}
	
	ai_accept = {
		base = 0

		modifier = {
			add = 100
			desc = GOLD_REASON
		}
	}
	
	ai_potential = {
		is_available_at_peace_ai_adult = yes
		ai_greed < medium_positive_ai_value
		short_term_gold >= bribe_council_member_cost
		NOT = {
			has_trait = greedy
		}
	}
	
	auto_accept = { always = yes }
	
	ai_min_reply_days = 1
	ai_max_reply_days = 1
	
	ai_will_do = { base = 100 }
}